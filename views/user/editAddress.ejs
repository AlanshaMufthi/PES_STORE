<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Address | 2ES</title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&family=Raleway:wght@300;400;500;600;700;800&family=Cinzel:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    * { font-family: 'Raleway', sans-serif; }
    .search-input { background:#f3f3f3;border:1px solid #ddd;border-radius:6px;padding:0.35rem 2rem 0.35rem 0.75rem;font-size:0.8rem;outline:none;width:180px;color:#555; }
    .search-wrap { position:relative; }
    .search-wrap svg { position:absolute;right:8px;top:50%;transform:translateY(-50%); }
    .icon-btn { background:none;border:none;cursor:pointer;padding:4px;border-radius:6px;display:flex;align-items:center;justify-content:center;color:#111;transition:background 0.15s; }
    .icon-btn:hover { background:#f3f3f3; }
    .cart-wrap { position:relative;display:flex;align-items:center; }
    .sidebar-link { transition:color 0.15s;cursor:pointer; }
    .sidebar-link:hover { color:#e63946; }
    .field-box { width:100%;padding:11px 16px;border:1.5px solid #d1d5db;border-radius:8px;background:#f9fafb;font-size:0.85rem;color:#374151;font-family:'Raleway',sans-serif;outline:none;transition:border 0.2s,background 0.2s; }
    .field-box:focus { border-color:#111;background:#fff; }
    .field-box.field-error { border-color:#f87171 !important; }
    .field-box::placeholder { color:#9ca3af; }
    textarea.field-box { resize:none;height:80px; }
    .error-msg { font-size:0.75rem;margin-top:4px;color:#f87171;font-weight:500;display:none; }
    .error-msg.show { display:block; }
    .type-radio { display:none; }
    .type-label { display:inline-flex;align-items:center;gap:7px;cursor:pointer;font-size:0.85rem;font-weight:600;color:#374151;padding:8px 18px;border:1.5px solid #d1d5db;border-radius:8px;transition:border-color 0.15s,background 0.15s; }
    .type-label:hover { border-color:#111; }
    .type-radio:checked + .type-label { border-color:#111;background:#f3f4f6;color:#111; }
    .type-dot { width:14px;height:14px;border-radius:50%;border:2px solid #9ca3af;display:inline-block;transition:all 0.15s; }
    .type-radio:checked + .type-label .type-dot { border-color:#111;box-shadow:inset 0 0 0 3px #fff,inset 0 0 0 6px #111; }
    .btn-submit { background:#111;color:#fff;font-size:0.88rem;font-weight:700;padding:11px 36px;border-radius:8px;border:none;cursor:pointer;font-family:'Raleway',sans-serif;transition:background 0.15s; }
    .btn-submit:hover { background:#333; }
    footer { background:#fff;border-top:1px solid #eee; }
  </style>
</head>
<body class="bg-white text-gray-900">

<%- include('../partials/user/header.ejs', { page: 'profile' }) %>

<main class="max-w-5xl mx-auto px-4 py-10">
  <div class="flex gap-8 items-start">

    <aside class="w-52 flex-shrink-0">
      <div class="flex flex-col items-center mb-6">
        <div class="w-24 h-24 rounded-full overflow-hidden border-2 border-gray-200 mb-3">
          <img src="<%= user && user.profileImg ? '/' + user.profileImg : '/images/default-avatar.png' %>" alt="Profile" onerror="this.src='/images/default-avatar.png'" class="w-full h-full object-cover" />
        </div>
        <div class="bg-gray-100 border border-gray-300 rounded px-4 py-1 text-sm font-semibold text-gray-700">
          <%= user ? user.firstName + ' ' + user.lastName : 'Customer Name' %>
        </div>
      </div>
      <div class="text-sm space-y-1">
        <p class="font-bold text-gray-900 mb-2">Manage My Account</p>
        <div class="pl-3 space-y-1.5">
          <a href="/profile" class="block text-gray-500 sidebar-link">My Profile</a>
          <a href="/addressBook" class="block font-semibold text-red-500 sidebar-link">Address Book</a>
        </div>
        <div class="pt-3 space-y-2.5">
          <a href="/orders" class="block font-bold text-gray-800 sidebar-link">My Orders</a>
          <a href="/privacy" class="block font-bold text-gray-800 sidebar-link">privacy</a>
          <a href="/wallet" class="block font-bold text-gray-800 sidebar-link">Wallet</a>
          <a href="/coupons" class="block font-bold text-gray-800 sidebar-link">Coupons</a>
          <a href="/wishlist" class="block font-bold text-gray-800 sidebar-link">My WishList</a>
          <a href="/logout" class="block font-bold text-red-500 sidebar-link">Logout</a>
        </div>
      </div>
    </aside>

    <div class="flex-1 border border-gray-200 rounded-xl p-8 bg-white shadow-sm">
      <div class="flex items-center justify-between mb-6 pb-4 border-b border-gray-100">
        <!-- ✏️ Changed title to Edit Address -->
        <p class="font-bold text-base" style="color:#e63946;">Edit Address</p>
        <p class="font-bold text-base text-gray-800">Personal Information</p>
      </div>

      <!-- ✏️ form id, action, and method updated for edit -->
      <form id="editAddressForm" action="/editAddress/<%= address._id %><%= typeof source !== 'undefined' && source ? '?source=' + source : '' %>" method="POST">
        <div class="space-y-5">

          <div class="grid grid-cols-2 gap-5">
            <div>
              <label class="block text-xs font-semibold text-gray-500 mb-1.5">First Name</label>
              <!-- ✏️ value pre-filled -->
              <input type="text" id="firstName" name="firstName" class="field-box"
                value="<%= address.firstName %>" placeholder="FirstName" />
              <p class="error-msg" id="firstNameErr"></p>
            </div>
            <div>
              <label class="block text-xs font-semibold text-gray-500 mb-1.5">Last Name</label>
              <!-- ✏️ value pre-filled -->
              <input type="text" id="lastName" name="lastName" class="field-box"
                value="<%= address.lastName %>" placeholder="LastName" />
              <p class="error-msg" id="lastNameErr"></p>
            </div>
          </div>

          <div>
            <label class="block text-xs font-semibold text-gray-500 mb-1.5">Phone</label>
            <!-- ✏️ value pre-filled -->
            <input type="text" id="phone" name="phone" class="field-box"
              value="<%= address.phone %>" placeholder="+91 12345 67890" />
            <p class="error-msg" id="phoneErr"></p>
          </div>

          <div>
            <label class="block text-xs font-semibold text-gray-500 mb-1.5">Address</label>
            <!-- ✏️ textarea content pre-filled (value goes between tags, not as attribute) -->
            <textarea id="addressLine" name="addressLine" class="field-box"
              placeholder="House Name , House Number, Locality"><%= address.addressLine %></textarea>
            <p class="error-msg" id="addressLineErr"></p>
          </div>

          <div class="grid grid-cols-2 gap-5">
            <div>
              <label class="block text-xs font-semibold text-gray-500 mb-1.5">District / Town</label>
              <!-- ✏️ value pre-filled -->
              <input type="text" id="town" name="town" class="field-box"
                value="<%= address.town %>" placeholder="eg : Ernakulam" />
              <p class="error-msg" id="townErr"></p>
            </div>
            <div>
              <label class="block text-xs font-semibold text-gray-500 mb-1.5">State</label>
              <!-- ✏️ value pre-filled -->
              <input type="text" id="state" name="state" class="field-box"
                value="<%= address.state %>" placeholder="eg : Kerala" />
              <p class="error-msg" id="stateErr"></p>
            </div>
          </div>

          <div class="grid grid-cols-2 gap-5">
            <div>
              <label class="block text-xs font-semibold text-gray-500 mb-1.5">Landmark <span class="text-gray-400 font-normal">(optional)</span></label>
              <!-- ✏️ value pre-filled -->
              <input type="text" id="landmark" name="landmark" class="field-box"
                value="<%= address.landmark || '' %>" placeholder="eg : Hospital" />
            </div>
            <div>
              <label class="block text-xs font-semibold text-gray-500 mb-1.5">Pin Code</label>
              <!-- ✏️ value pre-filled -->
              <input type="text" id="pincode" name="pincode" class="field-box"
                value="<%= address.pincode %>" placeholder="eg : 689230" />
              <p class="error-msg" id="pincodeErr"></p>
            </div>
          </div>

          <div class="flex items-center justify-between">
            <div>
              <label class="block text-xs font-semibold text-gray-500 mb-2">Address Type</label>
              <div class="flex gap-3">
                <!-- ✏️ checked state driven by saved addressType -->
                <input type="radio" id="typeHome" name="addressType" value="Home" class="type-radio"
                  <%= address.addressType === 'Home' ? 'checked' : '' %> />
                <label for="typeHome" class="type-label"><span class="type-dot"></span> Home</label>

                <input type="radio" id="typeWork" name="addressType" value="Work" class="type-radio"
                  <%= address.addressType === 'Work' ? 'checked' : '' %> />
                <label for="typeWork" class="type-label"><span class="type-dot"></span> Work</label>
              </div>
            </div>
            <!-- ✏️ button label updated -->
            <button type="submit" class="btn-submit">Save Changes</button>
          </div>

        </div>
      </form>
    </div>
  </div>
</main>

<%- include('../partials/user/footer.ejs') %>

<script>
  function showErr(id, errId, msg) { document.getElementById(id).classList.add('field-error'); const el = document.getElementById(errId); el.textContent = msg; el.classList.add('show'); }
  function clearErr(id, errId)    { document.getElementById(id).classList.remove('field-error'); const el = document.getElementById(errId); el.textContent = ''; el.classList.remove('show'); }

  [['firstName','firstNameErr'],['lastName','lastNameErr'],['phone','phoneErr'],
   ['addressLine','addressLineErr'],['town','townErr'],['state','stateErr'],['pincode','pincodeErr']
  ].forEach(([id, errId]) => document.getElementById(id).addEventListener('input', () => clearErr(id, errId)));

  // ✏️ form id updated to match editAddressForm
  document.getElementById('editAddressForm').addEventListener('submit', function(e) {
    let valid = true;
    const v = id => document.getElementById(id).value.trim();

    if (v('firstName').length < 3)           { showErr('firstName','firstNameErr','First name must be at least 3 characters'); valid = false; }
    if (v('lastName').length < 2)            { showErr('lastName','lastNameErr','Last name must be at least 2 characters'); valid = false; }
    if (!/^[0-9]{10}$/.test(v('phone')))     { showErr('phone','phoneErr','Enter a valid 10-digit phone number'); valid = false; }
    if (v('addressLine').length < 5)         { showErr('addressLine','addressLineErr','Please enter a valid address'); valid = false; }
    if (v('town').length < 2)                { showErr('town','townErr','Please enter your district / town'); valid = false; }
    if (v('state').length < 2)               { showErr('state','stateErr','Please enter your state'); valid = false; }
    if (!/^[0-9]{6}$/.test(v('pincode')))    { showErr('pincode','pincodeErr','Enter a valid 6-digit pin code'); valid = false; }

    if (!valid) e.preventDefault();
  });
</script>
</body>
</html>
